## Deploying application to Dapr on k8s
The daprfrontend images have already been built by previous run of docker-compose so we just use it here. 
Otherwise we can simply build them with 'docker build'